import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

//render App to ReactDOM here
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
